<div class="container">
  <div class="row">
    <p>Facture ref:  <%= @bill.reference %></p>
    <p>Date de publication : <%= @bill.publication.created_at.strftime("%d/%m/%Y") %></p>
    <p>Date de la facture  : <%= @bill.created_at.strftime("%d/%m/%Y") %></p>
    <p>Nom du client: <%= @bill.publication.customer %></p>
    <p>Titre de l'annonce: <%= @bill.publication.title %></p>
    <p>Date de publication de l'annonce: <%= @bill.publication.created_at.strftime("%d/%m/%Y") %>
</p>
    <p>Date de facturation<%= @bill.created_at.strftime("%d/%m/%Y") %></p>
    <p>Montant: <%= @bill.amount%> €</p>

    <%= link_to "Télécharger le pdf", bill_path(@bill, format: "pdf")  %>
  </div>
</div>
